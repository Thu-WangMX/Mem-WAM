defaults:
  - rmbench_battery_fullattention
  - _self_

train:
  _target_: fastwam.datasets.lerobot.multimodal_rate_controlled_dataset.MultimodalRateControlledRobotVideoDataset
  multimodal_manifest_path: ${oc.env:MULTIMODAL_RATE_CONTROLLED_MANIFEST}
  manifest_task: battery_try
  memory_tokens: 8
