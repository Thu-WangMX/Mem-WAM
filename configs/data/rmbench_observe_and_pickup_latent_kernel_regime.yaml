defaults:
  - rmbench_cover_blocks_fullattention
  - _self_

train:
  _target_: fastwam.datasets.lerobot.latent_kernel_regime_dataset.LatentKernelRegimeRobotVideoDataset
  latent_kernel_manifest_path: ${oc.env:LATENT_KERNEL_REGIME_MANIFEST}
  manifest_task: observe_and_pickup
  memory_tokens: 8
